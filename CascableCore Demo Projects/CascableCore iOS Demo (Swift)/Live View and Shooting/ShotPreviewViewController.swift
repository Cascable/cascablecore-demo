import UIKit

// This view controller is configured entirely in the storyboard.
@objc(ShotPreviewViewController) class ShotPreviewViewController: UIViewController {
    @IBOutlet var imageView: UIImageView!
}
